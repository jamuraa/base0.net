---
layout: default
title: base0.net - In Transition
---
.home
  .posts
    {% for post in site.posts limit:5 %}
    .post
      %h2
        %a{:href => "{{ post.url }}"} {{ post.title }}
      .date {{ post.date | date_to_string }}
      .entry
        {% if post.layout == 'amzmoviereview' %}
        {% include movie_fp_header.haml %}
        {% endif %}
        {{ post.content | before_fold }}
      %span.comment-link
        %a{:href => "{{ post.url }}/#disqus_thread"} Comments
    {% endfor %}
  %a{:href => "/archive.html"} &laquo; Article Archive

{% include disqus_comment_links.html %}
