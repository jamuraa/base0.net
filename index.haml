---
layout: default
title: base0.net - In Transition
---
.home
  %h1 Blog Posts
  .posts
    {% for post in site.posts limit:5 %}
    .post
      %h2 {{ post.title }}
      .date {{ post.date }}
      .entry {{ post.content }}

    {% comment %}
    %li
      %span {{ post.date | date_to_string }}
      &raquo;
      %a{:href => "{{ post.url }}"} {{ post.title }}
    {% endcomment %}
    {% endfor %}

